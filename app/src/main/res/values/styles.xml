<resources>
    <color name="primary" id="@color/primary">#59194B</color>
    <color name="primary_dark" id="@color/primary_dark">#59194B</color>
    <color name="accent" id="@color/accent">#59194B</color>
    <color name="text" id="@color/text">#F2F5A9</color>


    <!-- inherit from the material theme -->
    <style name="AppTheme" parent="android:ThemeOverlay.Material.Dark">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/text</item>
    </style>


</resources>